@using Microsoft.AspNetCore.Identity
@using pushistin.Areas.Identity.Data

@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager


@if (SignInManager.IsSignedIn(User))
{
   @* <li class="nav-item">
        <a id="manage" class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">@UserManager.GetUserName(User)</a>
    </li>
    <li class="nav-item">
        <form id="logoutForm" class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
            <button id="logout" type="submit" class="nav-link btn btn-link text-dark border-0">Выйти</button>
        </form>
    </li>*@
    <div class="header__top__right__auth">
        <a id="manage" asp-area="Identity" asp-page="/Account/Manage/Index"><i class="fa fa-user"> </i>@UserManager.GetUserName(User)</a>
    </div>
    <div class="header__top__right__auth">
        <form id="logoutForm" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
            <button id="logout" class="logout-btn" type="submit">выйти</button>
        </form>
    </div>
}
else
{
@*    <li class="nav-item">
        <a class="nav-link text-dark" id="register" asp-area="Identity" asp-page="/Account/Register">Регистрация</a>
    </li>
    <li class="nav-item">
        <a class="nav-link text-dark" id="login" asp-area="Identity" asp-page="/Account/Login">Вход</a>
    </li>*@
    <div class="header__top__right__auth">
        <a id="register" asp-area="Identity" asp-page="/Account/Register"><i class="fa fa-user"></i> Регистрация</a>
    </div>
    <div class="header__top__right__auth">
        <a id="login" asp-area="Identity" asp-page="/Account/Login"><i class="fa fa-user"></i> Вход</a>
    </div>
}
